#include <stdio.h>

int main(){
    char *increasingWords[];
    int i = 0;
    while(feof(stdin)){
        char *word;
        scanf("%s", word);
        if(monotonicallyIncreasingWord(word)){
            increasingWords[i]
        }
        
    }
    return 0;
}